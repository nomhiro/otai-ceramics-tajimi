---
/**
 * TrustBadges - 信頼性バッジコンポーネント
 * 購入者の信頼を獲得するためのバッジ表示
 */
import { FREE_SHIPPING_THRESHOLD } from '../data/shipping';

interface Props {
  variant?: 'default' | 'compact' | 'hero';
  class?: string;
}

const { variant = 'default', class: className = '' } = Astro.props;

const badges = [
  {
    icon: '🏭',
    title: '創業75年以上',
    subtitle: '1949年創業',
  },
  {
    icon: '✋',
    title: 'すべて手描き',
    subtitle: '職人の技',
  },
  {
    icon: '📦',
    title: `${(FREE_SHIPPING_THRESHOLD / 10000).toFixed(0)}万円以上送料無料`,
    subtitle: '全国配送',
  },
  {
    icon: '🔄',
    title: '8日間返品可',
    subtitle: '未使用品に限る',
  },
];
---

{variant === 'hero' && (
  <div class={`bg-gradient-to-r from-ai via-ai/95 to-ai py-6 shadow-lg ${className}`}>
    <div class="container-ma">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
        {badges.map((badge) => (
          <div class="text-center group">
            <div class="text-3xl mb-2 bg-hakuji/20 w-12 h-12 rounded-full flex items-center justify-center mx-auto group-hover:scale-110 transition-transform">{badge.icon}</div>
            <p class="font-serif text-hakuji font-medium text-sm md:text-base">{badge.title}</p>
            <p class="text-xs text-hakuji/70">{badge.subtitle}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
)}

{variant === 'default' && (
  <div class={`py-12 ${className}`}>
    <div class="container-ma">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        {badges.map((badge) => (
          <div class="bg-white p-6 rounded-lg border border-sumi/10 text-center hover:shadow-lg transition-shadow">
            <div class="text-4xl mb-3">{badge.icon}</div>
            <p class="font-serif text-ai font-medium text-lg">{badge.title}</p>
            <p class="text-sm text-sumi/60 mt-1">{badge.subtitle}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
)}

{variant === 'compact' && (
  <div class={`flex flex-wrap justify-center gap-4 md:gap-8 ${className}`}>
    {badges.map((badge) => (
      <div class="flex items-center gap-2">
        <span class="text-2xl">{badge.icon}</span>
        <span class="text-sm font-medium text-sumi">{badge.title}</span>
      </div>
    ))}
  </div>
)}
