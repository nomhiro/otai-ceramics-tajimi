---
/**
 * Header コンポーネント - 和モダンデザイン
 * 固定ヘッダー、スクロール時の影、ホバーアンダーライン
 */
const navItems = [
  { href: '/', label: 'ホーム' },
  { href: '/about/', label: '小田井窯について' },
  { href: '/products/', label: '商品一覧' },
  { href: '/gallery/', label: 'ギャラリー' },
  { href: '/learn/', label: '学ぶ' },
  { href: '/custom/', label: 'オリジナル制作' },
  { href: '/contact/', label: 'お問い合わせ' },
];
---

<header
  class="fixed top-0 left-0 right-0 z-50 bg-kinari/95 backdrop-blur-sm border-b border-sumi/10 transition-all duration-400"
  id="main-header"
>
  <div class="container-ma py-5">
    <div class="flex items-center justify-between">
      <!-- ロゴ（元サイトデザイン融合） -->
      <a href="/" class="flex items-center gap-3 md:gap-4 group">
        <!-- 徳利アイコン（元サイトより） -->
        <img
          src="/images/legacy/tokkuri-icon.gif"
          alt=""
          class="h-10 md:h-14 w-auto"
          aria-hidden="true"
        />
        <div class="flex flex-col">
          <h1 class="font-serif text-lg md:text-2xl font-medium tracking-wider text-ai">
            小田井窯
          </h1>
          <!-- OTAI CERAMICS TAJIMIタイトル（元サイトより） -->
          <img
            src="/images/legacy/otai-title.gif"
            alt="OTAI CERAMICS TAJIMI"
            class="h-4 md:h-5 w-auto mt-0.5"
          />
        </div>
      </a>

      <!-- デスクトップナビゲーション -->
      <nav class="hidden md:flex items-center gap-8">
        {navItems.map((item) => (
          <a
            href={item.href}
            class="nav-link font-serif text-sumi hover:text-ai transition-colors duration-300 relative py-2"
          >
            {item.label}
            <span class="nav-underline absolute bottom-0 left-0 w-full h-0.5 bg-ai scale-x-0 origin-left transition-transform duration-300"></span>
          </a>
        ))}
        <!-- 言語切り替え -->
        <a
          href="/en/"
          class="ml-2 px-3 py-1 text-sm border border-ai text-ai rounded hover:bg-ai hover:text-hakuji transition-colors"
        >
          EN
        </a>
      </nav>

      <!-- モバイルメニューボタン -->
      <button
        id="menu-toggle"
        class="md:hidden p-2 text-sumi hover:text-ai transition-colors"
        aria-label="メニューを開く"
        aria-expanded="false"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="1.5"
            d="M4 6h16M4 12h16M4 18h16"
          />
        </svg>
      </button>
    </div>

    <!-- モバイルナビゲーション -->
    <nav
      id="mobile-menu"
      class="hidden md:hidden mt-6 pb-6 border-t border-sumi/10 pt-6"
    >
      <div class="flex flex-col gap-5">
        {navItems.map((item) => (
          <a
            href={item.href}
            class="font-serif text-lg text-sumi hover:text-ai transition-colors"
          >
            {item.label}
          </a>
        ))}
        <a
          href="/en/"
          class="font-serif text-lg text-ai"
        >
          English
        </a>
      </div>
    </nav>
  </div>
</header>

<!-- ヘッダー分のスペーサー -->
<div class="h-20"></div>

<script>
  // モバイルメニュートグル
  const menuToggle = document.getElementById('menu-toggle');
  const mobileMenu = document.getElementById('mobile-menu');

  menuToggle?.addEventListener('click', () => {
    const isExpanded = mobileMenu?.classList.toggle('hidden') === false;
    menuToggle.setAttribute('aria-expanded', String(isExpanded));
  });

  // スクロール時のヘッダー挙動
  const header = document.getElementById('main-header');
  let lastScroll = 0;

  window.addEventListener('scroll', () => {
    const currentScroll = window.scrollY;

    if (currentScroll > 100) {
      header?.classList.add('shadow-sm');
    } else {
      header?.classList.remove('shadow-sm');
    }

    lastScroll = currentScroll;
  });
</script>

<style>
  /* ナビリンクホバー時にアンダーラインを表示 */
  .nav-link:hover .nav-underline {
    transform: scaleX(1);
  }
</style>
